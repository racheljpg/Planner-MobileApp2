<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_add">

    <fragment
        android:id="@+id/nav_add"
        android:name="org.wit.plannerapp.ui.add.AddFragment"
        android:label="@string/menu_add"
        tools:layout="@layout/fragment_add" />

    <fragment
        android:id="@+id/nav_list"
        android:name="org.wit.plannerapp.ui.list.ListFragment"
        android:label="@string/menu_list"
        tools:layout="@layout/fragment_list" />

    <fragment
        android:id="@+id/nav_listdetail"
        android:name="org.wit.plannerapp.ui.detail.ListDetailFragment"
        android:label="@string/menu_listdetail"
        tools:layout="@layout/fragment_listdetail" >
        <argument
            android:name="itemid"
            app:argType="string" />
    </fragment>
</navigation>